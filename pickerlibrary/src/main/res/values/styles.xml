<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="PickerDialog_anim_style">
        <item name="android:windowEnterAnimation">@anim/dialog_show_anim</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/dialog_hidden_anim</item>
        <!-- 指定消失的动画xml -->
    </style>

    <!-- <style name="PickerDialog" parent="@android:style/Theme.Dialog">-->
    <style name="PickerDialog" parent="@android:style/Theme.Holo.Dialog">
        <!-- 是否有边框 -->
        <item name="android:windowFrame">@null</item>
        <!--是否在悬浮Activity之上  -->
        <item name="android:windowIsFloating">true</item>
        <!--标题  -->
        <item name="android:windowNoTitle">true</item>
        <!--阴影  -->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
        <!-- 进入和退出的动画 -->
        <item name="android:windowAnimationStyle">@style/PickerDialog_anim_style</item>
        <!-- 点外边可以消失  -->
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>


    <style name="DatePickerWheel">

        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerInParent">true</item>

    </style>

    <style name="DatePickerWheelDivide">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:text">@string/divide_text</item>
        <item name="android:textColor">@color/color_text</item>
        <item name="android:gravity">center</item>
    </style>




</resources>